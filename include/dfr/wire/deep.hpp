// IEX DEEP 1.0: the message layer that rides on IEX-TP.
//
// Umbrella header. A caller that only needs the type table can include <dfr/wire/deep/constants.hpp> and pay
// for nothing else.
//
// Every field offset here was confirmed against a real capture before it was written down — see
// deep/constants.hpp for what that means and what evidence it rests on.

#ifndef DFR_WIRE_DEEP_HPP
#define DFR_WIRE_DEEP_HPP

#include <dfr/wire/deep/constants.hpp>
#include <dfr/wire/deep/encode.hpp>
#include <dfr/wire/deep/messages.hpp>

#endif  // DFR_WIRE_DEEP_HPP
